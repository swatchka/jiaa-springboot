apiVersion: apps/v1
kind: Deployment
metadata:
  name: analysis-service
  namespace: jiwon-tech
  labels:
    app: analysis-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: analysis-service
  template:
    metadata:
      labels:
        app: analysis-service
    spec:
      containers:
        - name: analysis-service
          image: <ACCOUNT_ID>.dkr.ecr.ap-northeast-2.amazonaws.com/analysis-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: db-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: analysis-service
  namespace: jiwon-tech
spec:
  selector:
    app: analysis-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP


